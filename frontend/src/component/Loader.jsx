// Functional component for rendering a loader (spinner)
const Loader = () => {
  return (
    <div className="animate-spin rounded-full h-16 w-16 border-t-4 border-teal-500 border-opacity-50"></div>
    // Div element styled as a spinning loader with a teal-colored border
  );
};

export default Loader;

